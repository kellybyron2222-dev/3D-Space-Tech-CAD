{
  "name": "spacetech-3d",
  "private": true,
  "version": "0.0.1",
  "description": "Space Tech 3D / SpaceForge — CubeSat Phase A systems workbook",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @spacetech/web dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "license": "Apache-2.0"
}
